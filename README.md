### Jupyter notebook using Docker
1. install [docker](https://docs.docker.com/engine/install/debian/)
2. run [install.sh](./install.sh)
3. run [getToken.sh](./getToken.sh) to get token needed to log into notebook
4. open browser and open link from last command
